name: "radare2"
command: "r2"
enabled: true
short_description: "Wrapper for the radare2 command"
description: |
  Wrapper for the $toolName command or embedded helper script.

  **Key Features:**
  - Structured parameters for repeatable execution
  - Suitable for security testing and automation workflows
  - Returns command output for further orchestration

  **Use Cases:**
  - Targeted validation and assessment tasks
  - Repeatable command execution from the agent runtime
  - Evidence collection in larger workflows

  **Key Features：**

  **Use Cases：**
  - Malware analysis
  - Firmware analysis
  - Exploit development

  **Notes：**
parameters:
  - name: "target"
    type: "string"
    description: |
      Target host or address.

      **Format：**

      **Example Values：**

      **Notes：**
      - Required parameter; cannot be empty
    required: true
    position: 1
    format: "positional"
  - name: "commands"
    type: "string"
    description: |
      Parameter for the radare2 tool.

      **Format：**


      **Example Values：**

      **Notes：**
    required: false
    flag: "-c"
    format: "flag"
  - name: "script_file"
    type: "string"
    description: |
      Parameter for the radare2 tool.

      **Format：**

      **Example Values：**
      - "/tmp/analyze.r2"
      - "./scripts/auto_analyze.r2"

      **Notes：**
    required: false
    flag: "-i"
    format: "flag"
  - name: "arch"
    type: "string"
    description: |
      Parameter for the radare2 tool.


      **Example Values：**
      - "x86"
      - "x86_64"
      - "arm"
      - "arm64"

      **Notes：**
    required: false
    flag: "-a"
    format: "flag"
  - name: "bits"
    type: "int"
    description: |
      Parameter for the radare2 tool.

      **Example Values：**

      **Notes：**
    required: false
    flag: "-b"
    format: "flag"
  - name: "auto_analyze"
    type: "bool"
    description: |
      Parameter for the radare2 tool.

      **Use Cases：**

      **Notes：**
    required: false
    flag: "-A"
    format: "flag"
    default: false
  - name: "debug"
    type: "bool"
    description: |
      Parameter for the radare2 tool.

      **Use Cases：**

      **Notes：**
    required: false
    flag: "-d"
    format: "flag"
    default: false
  - name: "quiet"
    type: "bool"
    description: |
      Parameter for the radare2 tool.

      **Use Cases：**

      **Notes：**
    required: false
    flag: "-q"
    format: "flag"
    default: false
  - name: "seek"
    type: "string"
    description: |
      Parameter for the radare2 tool.

      **Format：**

      **Example Values：**
      - "0x400000"
      - "main"
      - "0x1000"

      **Notes：**
    required: false
    flag: "-s"
    format: "flag"
  - name: "base_address"
    type: "string"
    description: |
      Parameter for the radare2 tool.

      **Format：**

      **Example Values：**
      - "0x400000"
      - "0x1000000"

      **Notes：**
    required: false
    flag: "-B"
    format: "flag"
  - name: "eval"
    type: "string"
    description: |
      Parameter for the radare2 tool.

      **Format：**
      - "key=value"

      **Example Values：**
      - "asm.arch=x86"
      - "asm.bits=32"
      - "scr.color=false"

      **Notes：**
    required: false
    flag: "-e"
    format: "flag"
  - name: "project"
    type: "string"
    description: |
      Parameter for the radare2 tool.

      **Format：**

      **Example Values：**
      - "./project.r2"
      - "/tmp/analysis.r2"

      **Notes：**
    required: false
    flag: "-p"
    format: "flag"
  - name: "additional_args"
    type: "string"
    description: |
      Additional arguments to append to the command.

      **Notes:**
      - Separate multiple arguments with spaces.
      - Ensure values are well-formed and safe for command execution.
      - This text is appended directly to the end of the command.


      **Example Values：**

      **Notes：**
      - Separate multiple arguments with spaces
      - This value is appended directly to the end of the command
    required: false
    format: "positional"
