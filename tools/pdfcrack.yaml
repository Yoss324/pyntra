name: "pdfcrack"
command: "pdfcrack"
enabled: true
short_description: "Wrapper for the pdfcrack command"
description: |
  Wrapper for the $toolName command or embedded helper script.

  **Key Features:**
  - Structured parameters for repeatable execution
  - Suitable for security testing and automation workflows
  - Returns command output for further orchestration

  **Use Cases:**
  - Targeted validation and assessment tasks
  - Repeatable command execution from the agent runtime
  - Evidence collection in larger workflows

  **Key Features：**
  - Brute-force attacks
  - Dictionary attacks

  **Use Cases：**
  - CTF competitions
  - Security testing
  - Digital forensics

  **Notes：**
  - Cracking time depends on password complexity
  - Use a wordlist to improve efficiency
parameters:
  - name: "file"
    type: "string"
    description: "Path to the file or directory to use."
    required: true
    position: 0
    format: "positional"
  - name: "wordlist"
    type: "string"
    description: "Path to the wordlist file."
    required: false
    flag: "-w"
    format: "flag"
  - name: "min_length"
    type: "int"
    description: "Parameter for the pdfcrack tool."
    required: false
    flag: "-n"
    format: "flag"
  - name: "max_length"
    type: "int"
    description: "Parameter for the pdfcrack tool."
    required: false
    flag: "-m"
    format: "flag"
  - name: "additional_args"
    type: "string"
    description: |
      Additional arguments to append to the command.

      **Notes:**
      - Separate multiple arguments with spaces.
      - Ensure values are well-formed and safe for command execution.
      - This text is appended directly to the end of the command.

      **Example Values：**
      - Add common examples based on the tool behavior

      **Notes：**
      - Separate multiple arguments with spaces
      - This value is appended directly to the end of the command
    required: false
    format: "positional"
