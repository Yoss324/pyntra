# SSE MCP Test Server

This helper command starts a minimal SSE-based MCP server for local integration testing.

## Usage
1. Run the command from the repository root.
2. Point a client or browser to the exposed SSE endpoint.
3. Use it to validate handshake flow, event streaming, and basic tool registration.
