# Reverse Shell MCP Server

This example MCP server exposes a tightly scoped reverse-shell helper for controlled lab workflows.

## Configuration
1. Review the Python script and restrict execution to approved environments.
2. Configure any required host, port, or authentication values before use.
3. Enable the server only when the surrounding assessment scope explicitly permits it.
