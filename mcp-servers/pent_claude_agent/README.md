# Pent Claude Agent MCP Bridge

This example integration shows how to expose an external Claude-style agent through an MCP server.

## Usage Notes
1. Review `pent_claude_agent_config.yaml` before enabling the bridge.
2. Keep API credentials and endpoints outside version control.
3. Validate requests and timeouts before using the bridge in larger workflows.
