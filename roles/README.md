# Role Files

Role definitions live in this directory as YAML files.

## Notes
- English role files are the preferred canonical versions.
- Compatibility aliases may exist for older role names.
- Keep `name`, `description`, `user_prompt`, and tool lists aligned when adding or editing roles.
